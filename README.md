## Setup

- `git clone https://github.com/TypeLuis/Boilerplate-class.git . && rm -rf .git`

- # Important to add values to `.env` and add `.env` inside of `.gitignore`

- `npm i`

- in `conn.js` change parameter to db name

- `npm run dev`